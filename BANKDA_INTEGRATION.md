# 뱅크다A 자동 입금 확인 연동 가이드

## 📌 작업 개요

무통장 입금 확인을 수동으로 하던 프로세스를 뱅크다A API를 통해 자동화했습니다.

### 변경 전
- 회원이 대회 신청 → `payment_status: 'pending'`
- 관리자가 계좌 확인 후 수동으로 → `'confirmed'`로 변경

### 변경 후
- 회원이 대회 신청 → `payment_status: 'pending'`
- 뱅크다A가 계좌 스크래핑 (5~10분마다)
- 입금자명 + 금액 자동 매칭
- API 호출로 자동 → `'confirmed'`로 변경 ✅

---

## 📁 구현된 파일 (3개)

### 1. 미확인주문리스트 API
**파일**: `src/app/api/bankda/pending-orders/route.ts`
- **메서드**: GET
- **용도**: 뱅크다A가 입금 매칭을 위해 pending 상태 주문 목록 조회
- **URL**: `https://your-domain.vercel.app/api/bankda/pending-orders`

**응답 형식**:
```json
{
  "orders": [
    {
      "order_id": "UUID",
      "buyer_name": "홍길동",
      "billing_name": "홍길동",
      "bank_account_no": "73491000872504",
      "bank_code_name": "하나은행",
      "order_price_amount": 30000,
      "order_date": "2025-12-09 17:59:01",
      "items": [
        {"product_name": "2025 서울 마라톤 10km"}
      ]
    }
  ]
}
```

---

### 2. 주문상세 API
**파일**: `src/app/api/bankda/order-detail/route.ts`
- **메서드**: POST
- **용도**: 뱅크다A가 특정 주문의 상세 정보 조회
- **URL**: `https://your-domain.vercel.app/api/bankda/order-detail`

**요청 형식**:
```json
{
  "order_id": "92098048-7051-41a0-9cca-b0b050442783"
}
```

**응답 형식**:
```json
{
  "order": {
    "order_id": "UUID",
    "buyer_name": "홍길동",
    "billing_name": "홍길동",
    "bank_account_no": "73491000872504",
    "bank_code_name": "하나은행",
    "order_price_amount": 30000,
    "order_date": "2025-12-09 17:59:01",
    "items": [
      {"product_name": "2025 서울 마라톤 10km"}
    ]
  }
}
```

---

### 3. 입금확인 API
**파일**: `src/app/api/bankda/confirm-payment/route.ts`
- **메서드**: POST
- **용도**: 뱅크다A가 매칭 성공한 주문의 입금 상태를 confirmed로 변경
- **URL**: `https://your-domain.vercel.app/api/bankda/confirm-payment`

**요청 형식**:
```json
{
  "requests": [
    {"order_id": "92098048-7051-41a0-9cca-b0b050442783"},
    {"order_id": "891ceab3-552b-4770-8b8d-de5571ad0423"}
  ]
}
```

**응답 형식**:
```json
{
  "return_code": 200,
  "description": "정상",
  "orders": [
    {"order_id": "UUID1", "description": "성공"},
    {"order_id": "UUID2", "description": "성공"}
  ]
}
```

**에러 응답**:
```json
{
  "return_code": 415,
  "description": "order_id 오류",
  "orders": [
    {"order_id": "UUID1", "description": "성공"},
    {"order_id": "UUID2", "description": "존재하지 않는 주문"}
  ]
}
```

---

## 🔄 전체 자동화 프로세스

```
[1] 회원이 대회 신청
   ↓ registrations 테이블에 저장
   ↓ payment_status: 'pending'

[2] 뱅크다A가 미확인주문리스트 API 호출 (주기적)
   → GET /api/bankda/pending-orders
   → pending 상태 주문 목록 받음

[3] 뱅크다A가 계좌 스크래핑 (5~10분마다)
   → 하나은행 734-910008-72504
   → 빠른계좌조회 서비스 이용

[4] 새 입금 발견
   → 입금자: 홍길동
   → 금액: 30,000원

[5] 뱅크다A 내부 자동 매칭
   → 입금자명(홍길동) + 금액(30,000) 일치하는 주문 찾기
   → order_id 매칭 성공!

[6] 입금확인 API 호출
   → POST /api/bankda/confirm-payment
   → {"requests": [{"order_id": "..."}]}

[7] API가 DB 업데이트
   → payment_status: 'pending' → 'confirmed'

[8] 완료!
   → 회원: 조회 탭에서 "입금 확인" 표시
   → 관리자: 대시보드에서 "입금확인" 상태 확인
```

---

## 🚀 배포 및 설정 (필수)

### STEP 1: 코드 배포

```bash
git add .
git commit -m "feat: 뱅크다A 완전 연동"
git push origin main
```

Vercel이 자동 배포합니다.

---

### STEP 2: 뱅크다A 관리페이지 URL 등록

배포 완료 후 https://a.bankda.com/ 접속하여 3개 URL 등록:

#### 1. 미확인 주문 리스트 API URL
```
https://your-domain.vercel.app/api/bankda/pending-orders
```

#### 2. 주문상세 API URL
```
https://your-domain.vercel.app/api/bankda/order-detail
```

#### 3. 입금확인 주문 API URL
```
https://your-domain.vercel.app/api/bankda/confirm-payment
```

**⚠️ 주의**: `your-domain`을 실제 Vercel 도메인으로 변경하세요!

---

### STEP 3: 뱅크다A 계좌 등록

1. **하나은행 인터넷뱅킹 접속**
2. **빠른계좌조회 서비스 활성화**
   - 계좌번호: 734-910008-72504
   - 예금주: (주)러닝브레이커
3. **뱅크다A 관리페이지에서 계좌 연동**
   - 은행: 하나은행
   - 계좌번호: 734-910008-72504
   - 인터넷뱅킹 ID/PW 입력

---

## 🧪 테스트 방법

### 1. 로컬 테스트

```bash
# 개발 서버 실행
npm run dev
```

#### A. 미확인주문리스트 API
```bash
curl http://localhost:3000/api/bankda/pending-orders
```

#### B. 주문상세 API
```bash
curl -X POST http://localhost:3000/api/bankda/order-detail \
  -H "Content-Type: application/json" \
  -d '{"order_id": "92098048-7051-41a0-9cca-b0b050442783"}'
```

#### C. 입금확인 API
```bash
curl -X POST http://localhost:3000/api/bankda/confirm-payment \
  -H "Content-Type: application/json" \
  -d '{"requests": [{"order_id": "92098048-7051-41a0-9cca-b0b050442783"}]}'
```

---

### 2. 뱅크다A 관리페이지 테스트

#### 미확인주문리스트
- 뱅크다A 관리페이지 → 미확인주문리스트 탭
- "테스트" 버튼 클릭
- pending 주문 목록 확인

#### 주문상세
- 주문상세 탭 → 테스트 입력:
```json
{"order_id": "92098048-7051-41a0-9cca-b0b050442783"}
```
- "테스트" 버튼 클릭

#### 입금확인
- 입금확인 탭 → 테스트 입력:
```json
{"requests": [{"order_id": "92098048-7051-41a0-9cca-b0b050442783"}]}
```
- "테스트" 버튼 클릭
- DB 확인: payment_status가 'confirmed'로 변경되었는지 확인

---

### 3. 실제 소액 입금 테스트

1. **테스트 신청 생성**
   - Supabase에서 테스트 신청 데이터 입력
   - 또는 실제로 대회 신청
   - 금액: 1,000원 (테스트용)

2. **실제 입금**
   - 하나은행 734-910008-72504
   - 입금자명: 신청 시 입력한 이름 (정확히!)
   - 금액: 1,000원

3. **5~10분 대기**
   - 뱅크다A가 계좌 스크래핑
   - 자동 매칭 및 API 호출

4. **DB 확인**
```sql
SELECT id, name, depositor_name, entry_fee, payment_status
FROM registrations
WHERE depositor_name = '테스트이름'
AND payment_status = 'confirmed';
```

✅ payment_status가 'confirmed'로 자동 변경되었다면 성공!

---

## 📊 주요 데이터 형식

### order_id
- **형식**: UUID
- **예시**: `92098048-7051-41a0-9cca-b0b050442783`
- **출처**: `registrations.id`

### 계좌번호
- **실제**: `734-910008-72504`
- **API 전송 시**: `73491000872504` (하이픈 제거)

### 날짜 형식
- **DB 저장**: `2025-12-09T17:59:01+09:00` (ISO 8601)
- **API 응답**: `2025-12-09 17:59:01` (T 제거)

### 품목명 생성 규칙
```typescript
const productName = `${대회명} ${참가종목}`
// 예: "2025 서울 마라톤 10km"
```

---

## 🔍 로그 확인

### Vercel 로그
```
[뱅크다A] 미확인 주문 5건 조회 완료
[뱅크다A] 주문 상세 조회: 홍길동 (92098048...)
[뱅크다A] 입금 확인 완료: 홍길동 (92098048...), 금액: 30000원
```

### Supabase 쿼리
```sql
-- 미확인 입금 조회
SELECT id, name, depositor_name, entry_fee, created_at
FROM registrations
WHERE payment_status = 'pending'
ORDER BY created_at DESC;

-- 최근 입금 확인된 내역
SELECT id, name, depositor_name, entry_fee, created_at
FROM registrations
WHERE payment_status = 'confirmed'
ORDER BY created_at DESC
LIMIT 10;

-- 1주일 이상 pending 상태 (수동 확인 필요)
SELECT id, name, depositor_name, entry_fee, created_at
FROM registrations
WHERE payment_status = 'pending'
AND created_at < NOW() - INTERVAL '7 days'
ORDER BY created_at DESC;
```

---

## ⚠️ 주의사항

### 1. 입금자명 정확히 일치 필요
- 신청 시 입력: "홍길동"
- 입금 시 입력: "홍길동" ✅
- 입금 시 입력: "홍 길 동" ❌ (띄어쓰기 다름)
- 입금 시 입력: "홍길순" ❌ (가족이 대신 입금)

불일치 시 자동 매칭 실패 → 수동 처리 필요

### 2. 금액 정확히 일치 필요
- 신청 금액: 30,000원
- 입금 금액: 30,000원 ✅
- 입금 금액: 29,500원 ❌ (수수료 제외)

불일치 시 자동 매칭 실패 → 수동 처리 필요

### 3. payment_status 확인
입금확인 API는 **반드시 'pending' 상태일 때만** 'confirmed'로 변경합니다.
- 이미 'confirmed' → 에러 반환
- 이미 'cancelled' → 에러 반환

### 4. 중복 주문
동명이인이 같은 금액을 입금한 경우:
- 뱅크다A가 먼저 신청한 사람에게 자동 매칭
- 또는 수동 처리 필요

---

## 🛠️ 트러블슈팅

### 문제 1: 입금했는데 자동 확인이 안됨

**원인**:
- 입금자명 불일치
- 금액 불일치
- 뱅크다A 계좌 스크래핑 오류
- API URL 미등록

**해결**:
1. DB에서 pending 상태 확인
```sql
SELECT * FROM registrations WHERE id = 'order_id';
```

2. 뱅크다A 관리페이지 확인
   - 미확인 주문 목록에 표시되는지
   - 계좌 스크래핑이 정상 작동하는지

3. Vercel 로그 확인
   - API 호출 기록 확인
   - 에러 로그 확인

---

### 문제 2: API 호출 시 401 오류

**원인**:
- 뱅크다A IP 화이트리스트 문제
- API URL 잘못 등록

**해결**:
- 뱅크다A 관리페이지에서 허용 IP 확인:
  - 13.209.86.108
  - 124.198.76.144/28

---

### 문제 3: DB 조회 오류

**원인**:
- Supabase RLS 정책
- 테이블 조인 오류

**해결**:
```sql
-- RLS 확인
SELECT * FROM pg_policies WHERE tablename = 'registrations';

-- 테이블 조인 확인
SELECT r.*, pg.name, c.title
FROM registrations r
LEFT JOIN participation_groups pg ON r.participation_group_id = pg.id
LEFT JOIN competitions c ON pg.competition_id = c.id
WHERE r.id = 'order_id';
```

---

## 📞 문의

### 뱅크다A 고객센터
- 웹사이트: https://a.bankda.com/
- 계정: runningbreaker10k@gmail.com
- 만료일: 2025-12-22

### 프로젝트 정보
- Git Repository: running-association
- Vercel 프로젝트: (배포 시 확인)
- Supabase 프로젝트: (프로젝트 URL 확인)

---

## 📝 체크리스트

배포 전:
- [ ] 3개 API 파일 생성 완료
- [ ] 로컬 테스트 완료
- [ ] Git commit & push

배포 후:
- [ ] Vercel 배포 확인
- [ ] 뱅크다A URL 3개 등록
- [ ] 뱅크다A 계좌 연동
- [ ] 뱅크다A 관리페이지 테스트
- [ ] 소액 실제 입금 테스트
- [ ] DB payment_status 변경 확인

운영:
- [ ] 매일 미확인 입금 확인
- [ ] 주 1회 수동 처리 필요 건 확인
- [ ] 월 1회 뱅크다A 연동 상태 확인

---

## 📅 작업 이력

- 2025-12-09: 뱅크다A 완전 연동 구현
  - 미확인주문리스트 API 추가
  - 주문상세 API 추가
  - 입금확인 API 구현 완료
  - 자동화 프로세스 완성

---

**이 문서는 뱅크다A 자동 입금 확인 기능의 모든 내용을 담고 있습니다.**
**추가 작업이나 문제 발생 시 이 문서를 참고하세요.**

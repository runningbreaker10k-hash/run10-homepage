# RUN10 Flash 기능 기획서

## 1. 개요

### 1.1 배경 및 문제점

**현재 런텐 회원의 사용 패턴**
```
회원가입 → (대회 오픈 시) 대회 신청 → (대회 종료) → ... → (다음 대회까지 방치)
```

| 문제점 | 설명 |
|--------|------|
| 낮은 재방문율 | 대회가 없는 기간에는 사이트 방문 동기 없음 |
| 회원 간 교류 부재 | 같은 회원이어도 서로 만날 기회 없음 |
| 회원 가치 제한 | 회원가입의 혜택이 대회 신청뿐 |

### 1.2 해결 방향

**Flash 기능 추가 후 기대하는 패턴**
```
회원가입 → 대회 신청 + 내 지역 러닝 모임 참여 → 지속적 사이트 방문
```

| 목표 | 기대 효과 |
|------|----------|
| 일상적 방문 유도 | "우리 동네에 오늘 뛸 사람 있나?" 확인 |
| 회원 간 네트워킹 | 같은 지역 러너들과 자연스럽게 교류 |
| 회원 가치 상승 | 대회 + 지역 모임 = 회원가입 이유 증가 |

### 1.3 핵심 컨셉

> **"전국 어디서든, 우리 동네 러닝 모임"**
>
> 당근마켓처럼 내 관심 지역을 설정하고
> 해당 지역의 모임을 바로 확인하고 참여

---

## 2. 핵심 설계: 구/군 기반 시스템

### 2.1 기본 원리

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│   모임 생성자                    참여자                  │
│   ──────────                    ──────                  │
│   [시/도] 선택                   /flash 페이지 내에서    │
│      ↓                          관심 [구/군] 설정        │
│   [구/군] 선택                       ↓                   │
│      ↓                          관심 지역 모임            │
│   상세 장소 입력                 우선 표시                │
│      ↓                                                  │
│   모임 등록                                             │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 2.2 행정구역 체계

| 단계 | 예시 | 용도 |
|------|------|------|
| 시/도 | 서울특별시, 대전광역시, 경기도... | 대분류 선택 |
| **구/군** | 서초구, 유성구, 수원시 장안구... | **핵심 필터 단위** |
| 상세 장소 | 반포한강공원 달빛광장 앞 | 자유 텍스트 입력 |

> **"구/군"이 핵심**: 관심 지역 설정, 필터의 기준 단위

---

## 3. 사용자 시나리오

### 3.1 모임 만들기 (생성자)

```
김러너(서울 거주)는 오늘 저녁 한강에서 뛰려고 한다.

1. 헤더 메뉴 → [런텐플래시] 클릭 (Phase 2에 헤더 노출 예정)
2. [모임 만들기] 클릭
3. 지역 선택: [서울특별시] → [서초구]
4. 상세 장소 입력: "반포한강공원 달빛광장 앞"
5. 제목, 시간, 인원 등 입력
6. 카카오 오픈채팅 URL 입력 (필수)
7. [모임 만들기] → 완료

→ 서울 서초구를 관심 지역으로 설정한 회원들에게 노출됨
```

### 3.2 관심 지역 설정 (참여자)

```
박조깅(대전 거주, 서울 출장 잦음)은 여러 지역에서 뛰고 싶다.

1. /flash 페이지 → [내 지역] 탭
2. 관심 지역 섹션에서 추가:
   - 대전광역시 유성구 (집 근처)
   - 대전광역시 서구 (회사 근처)
   - 서울특별시 강남구 (출장 시)
   ※ 최대 3개까지 등록 가능
3. 추가 즉시 해당 지역 모임 필터링 적용

→ 이제 이 3개 구의 모임만 [내 지역] 탭에 표시됨
```

### 3.3 모임 참여하기 (참여자)

```
박조깅이 런텐플래시에 접속한다.

1. 기본 화면: [내 지역] 탭 (관심 지역 유성구, 서구, 강남구 모임 표시)
2. "갑천 새벽 러닝" (유성구) 발견
3. 클릭 → 상세 확인 → [참여하기]
4. 참여 후 카카오 오픈채팅 버튼 활성화

→ 관심 지역 없이 접속하면 지역 추가 안내 후 빈 목록 표시
```

### 3.4 전체 지역 탐색

```
이달리기는 여행 중 다른 지역 모임이 궁금하다.

1. [전체 보기] 탭 클릭
2. [부산광역시] → [해운대구] 필터 선택 (선택 안 하면 전체 표시)
3. 해운대 러닝 모임들 확인
4. 마음에 드는 모임 참여
```

---

## 4. 기능 설계

### 4.1 핵심 기능 (MVP)

| 기능 | 설명 | 상태 |
|------|------|------|
| **관심 지역 설정** | /flash 페이지 내에서 관심 구/군 최대 3개 선택 | ✅ 완료 |
| **모임 목록 - 내 지역** | 관심 지역 모임만 표시 (없으면 빈 화면 + 안내) | ✅ 완료 |
| **모임 목록 - 전체** | 시/도·구/군 필터로 전체 검색 | ✅ 완료 |
| **진행/종료 필터** | 진행(기본) / 종료 탭. 진행은 날짜순 ASC, 종료는 DESC | ✅ 완료 |
| **모임 만들기** | 시/도 → 구/군 → 상세장소 | ✅ 완료 |
| **모임 상세/참여** | 상세 보기 + 참여/취소 | ✅ 완료 |
| **모임 수정** | 작성자 또는 관리자만 수정 가능 | ✅ 완료 |
| **모임 취소** | 작성자 또는 관리자만 취소 가능 | ✅ 완료 |
| **카카오 오픈채팅** | 필수 입력, 참여자에게만 버튼 표시 | ✅ 완료 |
| **모바일 반응형** | max-w-lg 기준 단일 컬럼 레이아웃 | ✅ 완료 |
| **히어로 섹션** | flash-hero-bg.jpg 배경, 다른 페이지와 동일 스타일 | ✅ 완료 |
| **내 Flash 모임 (마이페이지)** | 만든 모임 / 참여 예정 모임 탭 | ⬜ Phase 2 |
| **헤더 메뉴 노출** | 헤더에 "런텐플래시" 메뉴 추가 | ⬜ Phase 2 |

### 4.2 입력 항목 설계

**모임 생성/수정 시 입력**

| 항목 | 필수 | 입력 방식 | 예시 |
|------|------|----------|------|
| 제목 | 필수 | 텍스트 (50자) | "한강 야경 러닝" |
| **시/도** | 필수 | 드롭다운 선택 | 서울특별시 |
| **구/군** | 필수 | 드롭다운 선택 (시/도 종속) | 서초구 |
| **상세 장소** | 필수 | 텍스트 (100자) | "반포한강공원 달빛광장 앞" |
| 날짜 | 필수 | 생성: 오늘/내일/모레 선택 / 수정: date picker | 오늘 |
| 시간 | 필수 | 30분 단위 선택 | 19:00 |
| **최대 인원** | 필수 | 드롭다운 (2~10명) | 6명 |
| **카카오 오픈채팅 URL** | **필수** | URL 텍스트 입력 | https://open.kakao.com/... |
| 소개글 | 선택 | 텍스트 (200자) | "가볍게 뛰실 분~" |
| 예상 거리 | 선택 | 드롭다운 | 10km |
| 페이스 | 선택 | 드롭다운 | 중급 |
| 이미지 | 선택 | 단일 이미지 업로드 (5MB 이하) | 모임 장소 사진 |

> 최대 인원은 **2~10명 범위에서 생성자가 선택**. 설정된 인원 도달 시 선착순 자동 마감.  
> 수정 시 현재 참여 인원보다 적게 줄이는 것은 불가.

### 4.3 모임 상태 관리

```
[모집중] → [마감] → [완료]
   ↓
[취소됨]
```

| 상태 | 조건 | 배지 스타일 |
|------|------|------|
| 모집중 (open) | 인원 미달 & 시간 전 | 초록색 배지 |
| 마감 (closed) | **설정 인원 선착순 마감** (자동) | 노란색 배지 |
| 완료 (completed) | 모임 시간 경과 | 회색 배지 |
| 취소 (cancelled) | 주최자/관리자 취소 | 빨간색 배지 |

**목록 필터 기준**
- **진행**: `run_date >= 오늘` AND `status IN ('open', 'closed')` → 날짜/시간 오름차순
- **종료**: `run_date < 오늘` OR `status IN ('completed', 'cancelled')` → 날짜 내림차순

### 4.4 관리 권한

| 권한 | 모임장 (생성자) | 관리자 (admin) | 일반 회원 |
|------|:--------------:|:--------------:|:---------:|
| 모임 수정 | O | O | X |
| 모임 취소 | O | O | X |
| 참여자 강제 취소 | O | O | X |
| 참여/취소 | X (생성자는 자동 참여) | O | O |

> 참여는 선착순. 설정 인원 도달 즉시 자동 마감. 대기열 없음.

### 4.5 접근 권한

| 페이지 | 접근 조건 |
|--------|----------|
| /flash | 로그인 필요 |
| /flash/new | 로그인 필요 |
| /flash/[id] | 로그인 필요 |
| /flash/[id]/edit | 로그인 + 작성자 또는 관리자 |

---

## 5. 화면 설계

> 아이콘: 이모지 사용 금지. Lucide 아이콘으로 통일 (MapPin, Users, Calendar, Zap, MessageCircle 등)  
> 배지 스타일: 기존 대회 목록과 동일한 `rounded-full span` 사용

### 5.1 모임 목록 (/flash) - 내 지역 탭

```
┌────────────────────────────────────────────────────────────┐
│  [HERO] 런텐플래시 / 전국 어디서든, 우리 동네 러닝 모임     │
├────────────────────────────────────────────────────────────┤
│                                        [+ 모임 만들기]     │
├────────────────────────────────────────────────────────────┤
│  [내 지역 ━]  [전체 보기]                                   │
├────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 📍 관심 지역                          (1/3)          │  │
│  │ [서울 서초구 ×] [대전 유성구 ×]                       │  │
│  │                                                       │  │
│  │ [시/도 ▼] [구/군 ▼] [추가]  ← 3개 미만일 때만 표시   │  │
│  └──────────────────────────────────────────────────────┘  │
├────────────────────────────────────────────────────────────┤
│  [진행 ●] [종료]                                            │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  서울 서초구                                               │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ 한강 야경 러닝                            [모집중]   │ │
│  │ 반포한강공원 달빛광장 앞                              │ │
│  │ 1/29 19:00  5/6명  10km                              │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  ── 관심 지역 없을 때 ──                                   │
│  위에서 관심 지역을 추가하면 해당 지역 모임이 표시됩니다    │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### 5.2 모임 목록 (/flash) - 전체 보기 탭

```
┌────────────────────────────────────────────────────────────┐
│  [내 지역]  [전체 보기 ━]                                   │
├────────────────────────────────────────────────────────────┤
│  [전체 시/도 ▼] [전체 구/군 ▼]  ← 선택 시 필터 적용       │
├────────────────────────────────────────────────────────────┤
│  [진행 ●] [종료]                                            │
├────────────────────────────────────────────────────────────┤
│  서울 강남구                                               │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ 양재천 러닝                               [모집중]   │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

### 5.3 모임 만들기 (/flash/new)

```
┌────────────────────────────────────────────────────────────┐
│  Zap 런텐플래시 / 모임 만들기                    [← 뒤로]  │
├────────────────────────────────────────────────────────────┤
│  제목 *                                                    │
│  지역 *: [시/도 ▼] [구/군 ▼]                               │
│  상세 장소 *                                               │
│  날짜/시간 *: [오늘/내일/모레 ▼] [00:00~23:30 ▼]           │
│  최대 인원 *: [2~10명 ▼]  → 설정 인원 도달 시 선착순 마감  │
│  카카오 오픈채팅 URL * (참여자에게만 공개)                  │
│  ─────────── 선택 사항 ─────────────────────────          │
│  소개글 / 예상 거리 / 페이스 / 이미지                       │
│  ┌──────────────────────────────────────────────────────┐ │
│  │                    모임 만들기                         │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

### 5.4 모임 수정 (/flash/[id]/edit)

```
┌────────────────────────────────────────────────────────────┐
│  Zap 런텐플래시 / 모임 수정                      [← 뒤로]  │
├────────────────────────────────────────────────────────────┤
│  (기존 데이터 자동 채워짐)                                  │
│  날짜: date picker (자유 선택)                              │
│  최대 인원: 현재 참여 인원 미만 선택 불가                   │
│  ─────────── 선택 사항 ─────────────────────────          │
│  ┌──────────────────────────────────────────────────────┐ │
│  │                    수정 완료                           │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

### 5.5 모임 상세 (/flash/[id])

```
┌────────────────────────────────────────────────────────────┐
│  Zap 런텐플래시     [수정] [취소] [← 목록]  ← 작성자만     │
├────────────────────────────────────────────────────────────┤
│  [이미지 - 있을 때만]                                       │
│  한강 야경 러닝                               [모집중]      │
│                                                            │
│  서울특별시 서초구 / 반포한강공원 달빛광장 앞               │
│  2026.01.29 19:00                                          │
│  5/6명 참여 중 (선착순 마감)                                │
│  10km · 중급                                               │
│  주최: 김러너                                              │
│                                                            │
│  소개글 내용...                                            │
│                                                            │
│  참여자 (5/6명)                                            │
│  1 김러너(주최)  2 박조깅  3 이달리기  ...                  │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  [MessageCircle] 카카오 오픈채팅 참여하기  ← 참여자만 │ │
│  └──────────────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  참여하기  ← 미참여 + open 상태                       │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

---

## 6. 데이터 설계

### 6.1 테이블 구조

> regions 별도 테이블 없음. sido/sigungu를 문자열로 직접 저장.

**flash_runs (모임)**

| 컬럼 | 타입 | 필수 | 설명 |
|------|------|------|------|
| id | UUID | 필수 | PK |
| creator_id | UUID | 필수 | 주최자 (users.id FK) |
| sido | VARCHAR(20) | 필수 | 시/도 |
| sigungu | VARCHAR(20) | 필수 | 구/군 |
| location_detail | VARCHAR(100) | 필수 | 상세 장소 |
| title | VARCHAR(50) | 필수 | 모임 제목 |
| run_date | DATE | 필수 | 모임 날짜 |
| run_time | TIME | 필수 | 모임 시간 |
| max_participants | INTEGER | 필수 | 최대 인원 (2~10) |
| current_participants | INTEGER | 필수 | 현재 참여 인원 (기본값 1) |
| description | TEXT | 선택 | 소개글 |
| distance | VARCHAR(10) | 선택 | 예상 거리 |
| pace | VARCHAR(10) | 선택 | 페이스 |
| status | VARCHAR(20) | 필수 | 상태 (기본값 'open') |
| image_url | TEXT | 선택 | 모임 이미지 URL |
| kakao_chat_url | VARCHAR(500) | **필수** | 카카오 오픈채팅 URL |
| created_at | TIMESTAMPTZ | 필수 | 생성일시 |

**flash_participants (참여자)**

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID | PK |
| flash_run_id | UUID | 모임 ID (FK) |
| user_id | UUID | 참여자 ID (FK) |
| joined_at | TIMESTAMPTZ | 참여일시 |

**user_favorite_regions (관심 지역)**

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID | PK |
| user_id | UUID | 사용자 ID (FK) |
| sido | VARCHAR(20) | 시/도 |
| sigungu | VARCHAR(20) | 구/군 |
| created_at | TIMESTAMPTZ | 등록일시 |

> **최대 3개** 제한 (UI 레벨에서 관리, DB UNIQUE(user_id, sido, sigungu))

### 6.2 SQL 스키마

```sql
CREATE TABLE flash_runs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  creator_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  sido VARCHAR(20) NOT NULL,
  sigungu VARCHAR(20) NOT NULL,
  location_detail VARCHAR(100) NOT NULL,
  title VARCHAR(50) NOT NULL,
  run_date DATE NOT NULL,
  run_time TIME NOT NULL,
  max_participants INTEGER NOT NULL,
  current_participants INTEGER NOT NULL DEFAULT 1,
  description TEXT,
  distance VARCHAR(10),
  pace VARCHAR(10),
  status VARCHAR(20) NOT NULL DEFAULT 'open',
  image_url TEXT,
  kakao_chat_url VARCHAR(500) NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  CONSTRAINT valid_status CHECK (status IN ('open', 'closed', 'completed', 'cancelled')),
  CONSTRAINT valid_max_participants CHECK (max_participants BETWEEN 2 AND 10)
);

CREATE TABLE flash_participants (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  flash_run_id UUID NOT NULL REFERENCES flash_runs(id) ON DELETE CASCADE,
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  joined_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(flash_run_id, user_id)
);

CREATE TABLE user_favorite_regions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  sido VARCHAR(20) NOT NULL,
  sigungu VARCHAR(20) NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(user_id, sido, sigungu)
);

CREATE INDEX idx_flash_runs_sido_sigungu ON flash_runs(sido, sigungu);
CREATE INDEX idx_flash_runs_date ON flash_runs(run_date);
CREATE INDEX idx_flash_runs_status ON flash_runs(status);
CREATE INDEX idx_flash_runs_creator ON flash_runs(creator_id);
CREATE INDEX idx_flash_participants_run ON flash_participants(flash_run_id);
CREATE INDEX idx_flash_participants_user ON flash_participants(user_id);
CREATE INDEX idx_user_favorite_regions_user ON user_favorite_regions(user_id);
```

---

## 7. 페이지 구조

```
/flash              → 모임 목록 (관심 지역 우선 / 전체 보기) + 관심지역 설정 inline
/flash/new          → 모임 만들기 (로그인 필요)
/flash/[id]         → 모임 상세 + 참여/취소 (로그인 필요)
/flash/[id]/edit    → 모임 수정 (작성자 또는 관리자만)

Phase 2 예정:
  마이페이지 탭 추가:
    - Flash 모임 탭  → 내가 만든 모임 / 참여 예정 모임
  헤더에 "런텐플래시" 메뉴 추가
```

---

## 8. 개발 현황

### 8.1 완료 목록

| 항목 | 파일 | 비고 |
|------|------|------|
| DB 테이블 생성 | Supabase | RLS 없음 |
| 히어로 섹션 | /flash/page.tsx | flash-hero-bg.jpg |
| 내 지역 탭 | /flash/page.tsx | 관심지역 없으면 빈 목록 + 안내 |
| 전체 보기 탭 | /flash/page.tsx | 시/도·구/군 필터 |
| 진행/종료 필터 | /flash/page.tsx | 기본값: 진행 |
| 관심 지역 inline | /flash/page.tsx | 항상 표시, 최대 3개 |
| 모임 만들기 | /flash/new/page.tsx | 로그인 필요 |
| 모임 상세/참여 | /flash/[id]/page.tsx | 로그인 필요 |
| 모임 수정 | /flash/[id]/edit/page.tsx | 작성자·관리자만 |
| 모임 취소 | /flash/[id]/page.tsx | 작성자·관리자만 |
| 카카오 오픈채팅 | /flash/[id]/page.tsx | 참여자에게만 표시 |
| 모바일 반응형 | 전체 flash 페이지 | max-w-lg 기준 |
| 한국 지역 데이터 | /lib/korea-regions.ts | 전국 시/도·구/군 |
| 타입 정의 | /lib/supabase.ts | flash_runs, flash_participants, user_favorite_regions |

### 8.2 Phase 2 예정

| 항목 | 설명 |
|------|------|
| 헤더 "런텐플래시" 메뉴 | 현재는 직접 URL 접속만 가능 |
| 마이페이지 Flash 탭 | 내가 만든 / 참여한 모임 목록 |
| 앱 푸시알림 | 관심 지역 새 모임 등록 시 OneSignal 발송 |
| Run-Mileage 시스템 | 참여 시 km 단위 마일리지 |
| 지도 보기 | 카카오맵으로 모임 위치 표시 |
| 댓글 | 모임 상세에 댓글 |
| 모임 후 평가 | 참여자 상호 평가, 노쇼 페널티 |

---

## 9. 성공 지표

| 지표 | 목표 (1개월 후) | 측정 방법 |
|------|----------------|----------|
| 관심 지역 설정 비율 | 방문자의 70% | user_favorite_regions 등록 비율 |
| 모임 생성 수 | 주 15개 이상 | flash_runs 카운트 |
| 참여 신청 수 | 주 40건 이상 | flash_participants 카운트 |
| 지역 분포 | 5개 이상 시/도 활성화 | 지역별 모임 수 |

---

## 10. 참여자 후기 기능 (좋아요 & 신고)

### 10.1 개요

모임이 종료된 이후, 실제 참여자들끼리 서로를 평가하는 후기 시스템.

- **좋아요** (긍정): 버튼 토글 방식, 폼 없음, 누적 카운트
- **신고** (부정): 폼 작성 후 관리자 확인·처리

| 항목 | 좋아요 | 신고 |
|------|--------|------|
| 평가 대상 | 같은 모임의 **다른 참여자** (본인 제외) | 같은 모임의 **다른 참여자** (본인 제외) |
| 평가 가능자 | 해당 모임 참가자 (flash_participants) | 해당 모임 참가자 (flash_participants) |
| 가능 시점 | 모임 종료 후 (completed / cancelled) | 모임 종료 후 (completed / cancelled) |
| 중복 방지 | 동일 모임에서 같은 상대에게 1회 (취소 후 재누름 가능) | 동일 모임에서 같은 상대에게 1회 |
| 주최자·관리자 | 평가 가능 (본인 제외 조건만 동일) | 평가 가능 (본인 제외 조건만 동일) |

---

### 10.2 신고 사유 목록

> DB에 코드(영문 키) 없이 **한국어 텍스트를 그대로 저장**. 선택지는 고정 목록으로 관리.

| 저장 값 (선택 옵션 텍스트) |
|--------------------------|
| 욕설 및 비방 |
| 성희롱 및 스토킹 |
| 과도한 연락 |
| 영업행위 및 종교 권유 |
| 정치 활동 |
| 사기 |
| 폭행 및 위협 |
| 허위 정보 등록 |
| 반복적인 노쇼 |
| 음주 흡연 강요 및 불편 유발 |
| 기타 운영정책 위반 행위 |

---

### 10.3 화면 설계

**모임 상세 참여자 목록 (`/flash/[id]`) — 종료 상태일 때**

```
┌──────────────────────────────────────────────┐
│  참여자 (6/6명)                               │
│  ┌────────────────┐  ┌────────────────┐      │
│  │ 🐆 김러너(주최)│  │ 🐎 러*맨       │      │
│  │                │  │  👍 3  🚨 신고 │ ← 종료 후만 │
│  └────────────────┘  └────────────────┘      │
│  ┌────────────────┐  ┌────────────────┐      │
│  │ 🐺 이*기       │  │ 🐢 박*조       │      │
│  │  👍 1  🚨 신고 │  │  👍 5  🚨 신고 │      │
│  └────────────────┘  └────────────────┘      │
└──────────────────────────────────────────────┘
```

> - 본인 카드에는 좋아요·신고 버튼 미표시
> - 이미 좋아요한 참여자: 버튼 활성(색상) 표시, 누르면 취소
> - 이미 신고한 참여자: "신고됨" 비활성 텍스트로 표시

**신고 페이지 (`/flash/[id]/report/[targetUserId]`)**

```
┌──────────────────────────────────────────────┐
│  ! 런텐플래시 / 신고하기              [← 뒤로]│
├──────────────────────────────────────────────┤
│  건전한 러닝 문화를 위해                      │
│  신고 내용을 확인 후 접수해주세요.            │
├──────────────────────────────────────────────┤
│  신고 대상 참여자                             │
│  ┌──────────────────────────────────────────┐ │
│  │ 모임    한강 야경 러닝                    │ │
│  │ 대상    러*맨                             │ │
│  │ 모임일  2026.07.24                        │ │
│  └──────────────────────────────────────────┘ │
├──────────────────────────────────────────────┤
│  신고 사유 *  [신고 사유를 선택해 주세요 ▼]  │
├──────────────────────────────────────────────┤
│  신고 내용 *                                  │
│  [신고의 사유와 발생 상황을 구체적으로...]    │
├──────────────────────────────────────────────┤
│  전화번호 *  010-0000-0000                    │
├──────────────────────────────────────────────┤
│  증빙자료 (선택)                              │
│  [이미지 업로드 JPG, PNG, GIF 최대 5MB]       │
├──────────────────────────────────────────────┤
│  ☐ 신고 내용을 사실에 근거하여 작성했음을     │
│     확인합니다. 허위 신고 시 이용이 제한됩니다 │
├──────────────────────────────────────────────┤
│  [취소]                    [신고 접수하기]    │
└──────────────────────────────────────────────┘
```

**관리자 신고 목록 (`/admin/flash-reports`)**

```
┌────────────────────────────────────────────────────────┐
│  플래시 신고 관리                                       │
│  [전체] [접수] [확인중] [처리완료] [기각]   검색:____  │
├────────────────────────────────────────────────────────┤
│  신고일      모임명          신고자  피신고자  사유  상태│
│  2026.07.24  한강 야경 러닝  김*너   러*맨   노쇼  [접수]│
└────────────────────────────────────────────────────────┘
```

**관리자 신고 상세**

```
┌──────────────────────────────────────────────────────┐
│  신고 상세                                            │
│  모임: 한강 야경 러닝 (→ 모임 바로가기)              │
│  신고자: 김*너 / 피신고자: 러*맨 / 신고일: 07.24     │
│  사유: 반복적인 노쇼                                  │
│  내용: (신고 내용 전문)                               │
│  전화번호: 010-****-1234  /  증빙자료: [이미지]       │
│  ───────────────────────────────────────────────────  │
│  처리 상태: [접수 ▼]  →  확인중 / 처리완료 / 기각    │
│  관리자 메모: _______________________________________  │
│  [저장]                                               │
└──────────────────────────────────────────────────────┘
```

---

### 10.4 데이터 설계

**flash_likes (좋아요)**

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID | PK |
| flash_run_id | UUID | 대상 모임 ID (FK → flash_runs) |
| liker_id | UUID | 좋아요 누른 사람 (FK → users) |
| liked_user_id | UUID | 좋아요 받은 사람 (FK → users) |
| created_at | TIMESTAMPTZ | 좋아요 일시 |

```sql
CREATE TABLE flash_likes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  flash_run_id UUID NOT NULL REFERENCES flash_runs(id) ON DELETE CASCADE,
  liker_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  liked_user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(flash_run_id, liker_id, liked_user_id)  -- 중복 좋아요 방지
);

CREATE INDEX idx_flash_likes_run ON flash_likes(flash_run_id);
CREATE INDEX idx_flash_likes_liked ON flash_likes(liked_user_id);  -- 누적 카운트 집계용
```

**flash_reports (신고)**

| 컬럼 | 타입 | 필수 | 설명 |
|------|------|------|------|
| id | UUID | 필수 | PK |
| flash_run_id | UUID | 필수 | 신고 발생 모임 ID (FK → flash_runs) |
| reporter_id | UUID | 필수 | 신고자 ID (FK → users) |
| target_user_id | UUID | 필수 | 피신고자 ID (FK → users) |
| reason | VARCHAR(50) | 필수 | 신고 사유 (한국어 텍스트) |
| content | TEXT | 필수 | 신고 내용 |
| phone | VARCHAR(20) | 필수 | 신고자 연락처 |
| evidence_url | TEXT | 선택 | 증빙자료 이미지 URL |
| status | VARCHAR(20) | 필수 | 처리 상태 (기본값 'pending') |
| admin_note | TEXT | 선택 | 관리자 처리 메모 |
| created_at | TIMESTAMPTZ | 필수 | 신고일시 |

```sql
CREATE TABLE flash_reports (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  flash_run_id UUID NOT NULL REFERENCES flash_runs(id) ON DELETE CASCADE,
  reporter_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  target_user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  reason VARCHAR(50) NOT NULL,
  content TEXT NOT NULL,
  phone VARCHAR(20) NOT NULL,
  evidence_url TEXT,
  status VARCHAR(20) NOT NULL DEFAULT 'pending',
  admin_note TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  CONSTRAINT valid_reason CHECK (reason IN (
    '욕설 및 비방','성희롱 및 스토킹','과도한 연락','영업행위 및 종교 권유',
    '정치 활동','사기','폭행 및 위협','허위 정보 등록','반복적인 노쇼',
    '음주 흡연 강요 및 불편 유발','기타 운영정책 위반 행위'
  )),
  CONSTRAINT valid_status CHECK (status IN ('pending','reviewing','resolved','dismissed')),
  UNIQUE(flash_run_id, reporter_id, target_user_id)  -- 동일 모임에서 같은 대상 중복 신고 방지
);

CREATE INDEX idx_flash_reports_run ON flash_reports(flash_run_id);
CREATE INDEX idx_flash_reports_target ON flash_reports(target_user_id);
CREATE INDEX idx_flash_reports_status ON flash_reports(status);
```

**처리 상태(status) 값**

| 값 | 의미 |
|----|------|
| pending | 접수 (기본값) |
| reviewing | 확인중 |
| resolved | 처리완료 |
| dismissed | 기각 |

---

### 10.5 페이지 및 파일 구조

```
/flash/[id]/report/[targetUserId]  → 신고 페이지 (참가자 + 종료 상태 + 본인 제외)
/admin/flash-reports               → 관리자 신고 목록
```

| 파일 | 역할 |
|------|------|
| src/app/flash/[id]/report/[targetUserId]/page.tsx | 신고 접수 폼 |
| src/app/admin/flash-reports/page.tsx | 관리자 신고 목록·처리 |

---

### 10.6 구현 체크리스트

**DB**
- [ ] Supabase에서 `flash_likes` 테이블 생성
- [ ] Supabase에서 `flash_reports` 테이블 생성 (target_user_id 포함)
- [ ] `competition-images` 버킷에 증빙자료 업로드 허용 (기존 버킷 재사용)

**모임 상세 (`/flash/[id]`) — 참여자 목록**
- [ ] 종료 상태(completed/cancelled)일 때 좋아요·신고 버튼 표시
- [ ] 본인 카드에는 버튼 미표시
- [ ] 좋아요 버튼: 토글(누르면 INSERT / 다시 누르면 DELETE), 카운트 표시
- [ ] 이미 좋아요한 경우 버튼 활성 색상 표시
- [ ] 신고 버튼: `/flash/[id]/report/[targetUserId]`로 이동
- [ ] 이미 신고한 경우 "신고됨" 비활성 텍스트 표시
- [ ] RunCard(목록 카드)의 `ChevronRight`(>) 아이콘 제거

**신고 페이지 (`/flash/[id]/report/[targetUserId]`)**
- [ ] 모임명·피신고자(마스킹)·모임일 상단 표시
- [ ] 신고 사유 드롭다운
- [ ] 신고 내용 textarea
- [ ] 전화번호 입력 (010-XXXX-XXXX 형식 검증)
- [ ] 증빙자료 이미지 업로드 (선택, ImageUpload 컴포넌트 재사용)
- [ ] 동의 체크박스 (미동의 시 제출 불가)
- [ ] 접근 제어 — 미충족 시 `/flash/[id]`로 redirect
  - 로그인 상태
  - 해당 모임 참가자 (flash_participants 조회)
  - 모임 종료 상태 (completed / cancelled)
  - targetUserId ≠ 본인
  - targetUserId도 해당 모임 참가자일 것
- [ ] 중복 신고 체크 → 이미 신고한 경우 안내 메시지
- [ ] 제출 후 완료 메시지 → 모임 상세로 이동

**관리자 페이지 (`/admin/flash-reports`)**
- [ ] 신고 목록 테이블 (신고일, 모임명, 신고자, 피신고자, 사유, 상태)
- [ ] 상태별 필터 탭 (전체/접수/확인중/처리완료/기각)
- [ ] 키워드 검색
- [ ] 상세: 신고 내용·증빙자료·연락처 확인, 처리 상태 변경, 관리자 메모
- [ ] 대상 모임 바로가기 링크

---

### 10.7 포인트 시스템 연동 (보류)

> 아직 확정 전. 아래는 기획 방향만 기록.

| 이벤트 | 포인트 변동 |
|--------|------------|
| 좋아요 받음 | + (미정) |
| 신고 → 처리완료 (제재) | - (미정) |
| 신고 → 기각 | 없음 |

- 포인트는 `users` 테이블에 `flash_point` 컬럼 추가 또는 별도 `flash_point_logs` 테이블로 관리
- 마이페이지에서 누적 포인트 및 내역 확인
- 사용처(할인, 혜택 등) 미정

---

**문서 버전**: 7.0  
**최종 수정**: 2026-07-13  
**변경 이력**:
- v6.2: 카카오 URL 필수화, 최대 인원 2~10명 선택, 선착순 마감, 관리 권한 추가
- v7.0: 구현 기준 전면 반영 — 관심지역 위치 변경(마이페이지→/flash inline), 최대 3개 제한, 날짜필터→진행/종료 필터, /flash/[id]/edit 추가, Phase 2 항목 분리
- v7.1: 신고하기 기능 계획 추가 (섹션 10)
- v7.2: 신고 조건 수정 — 참가자만, 종료 상태(completed/cancelled)일 때만 가능
- v7.3: 신고 사유 코드 제거(한국어 텍스트 직접 저장), 기타 항목 삭제, ⋮ 메뉴 방식으로 변경, RunCard > 제거
- v8.0: 섹션 10 전면 개편 — 평가 대상 모임→참여자, 좋아요 기능 추가, 포인트 시스템 보류 기록
